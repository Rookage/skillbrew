# 项目章程 PROJECT_CHARTER（核心锚定文件）

> 本文件是项目的"北极星"，作为开发与决策的统一基准。
> 状态：核心能力已落地 · 8 步管线端到端跑通（B站 / 抖音双源）· 6 个 MCP 服务器完成真实安装并通过冒烟测试（含 1 个 AI 推断安装）· 通用安装器（四级降级链：缓存→目录→AI推断→试跑→弹窗）· 双运行时中立（Claude Code / Codex CLI）。
> 具体能力清单与计数以本地 `RECORD.md` 台账为准，不在文档中硬编码。

---

## 0. 一句话定位

> **看过、收藏过、装过但忘了，不等于真正拥有一个能力。**
>
> 技能酿造局：把散落在视频、网页、README 和推荐清单里的 Agent 能力，酿成可验证、可安装、可追踪的一套本地能力库。

一个 **AI Agent 能力管理器**：把视频 / 报告 / 文件 / 网页丢进去，自动消化成「AI 可执行计划」，授权后按步安装成 AI 的真实能力（Skill / MCP / 代码 / 配置），并维护一张可去重、可累加、可视化的能力台账。开源，本地优先，面向 Claude Code / Codex / MCP 工具链。

## 1. 核心概念（术语统一）

| 术语 | 含义 | 类比 |
|------|------|------|
| **素材 Source** | 输入的视频 / 报告 / 文件 / 网页（不止视频） | 原料（葡萄） |
| **消化 Digest** | 把素材转成结构化理解（含画面，非仅字幕） | 发酵过程 |
| **执行计划 Plan** | 消化后产出的「AI 可执行步骤清单」，每步可授权 | 酿造配方 |
| **能力产物 Artifact** | 计划落地后的实物：Skill / MCP / 代码 / 配置 / Prompt / CLI 工具 / Docker 镜像 / API 服务 / 数据集。形态由计划内容决定，不预设 | 成品酒 |
| **能力台账 Registry** | 所有已安装能力的结构化登记表（去重/归并/分类/查询/移除） | 酒窖清单 |
| **安装记录 Record** | 每次执行留一份 README 式可分享记录（装了什么、怎么装、可移除） | 品鉴笔记 |
| **看板 Dashboard** | 累加可视化（本次能力 + 历史归并后的整体能力） | 酒窖全景图 |
| **溯源 Verify** | 介绍到 GitHub/Anthropic/论文的素材，回原厂取一手资料，不只用二手 | 产地认证 |

> **酿造隐喻**：素材是葡萄，消化是发酵，计划是配方，产物是成品酒，台账是酒窖清单，看板是全景图。每一步都可追溯、可审计、可回滚。

## 2. 痛点 → 目标

### 痛点链条

你遇到过吗？

- 刷到「Claude Code 必装 6 个 MCP」视频，收藏了，然后...忘了
- 想装的时候，又要重新看视频、找链接、配环境
- 装完发现：「等等，我之前是不是装过？」
- 装了一堆，越来越臃肿，启动越来越慢
- 最后：装是装了，但怎么用？文档在哪？

**从「信息过载」到「能力落地」，中间差一个酿造局。**

### 目标

- **去掉人工消化**：素材 → 自动消化 → 可执行计划 → 授权安装 → 真实能力
- **去重防臃肿**：三层从严判定（名字 / 描述 / 语义），不重复装
- **清楚知道**：台账 RECORD.md + 看板 DASHBOARD.md，装了哪些能力、每个怎么调（以本地台账为准，不硬编码能力数）
- **每次留记录**：可分享、可回滚、可审计
- **开源共建**：代码开源，欢迎贡献

## 3. 8 步管线（精炼后）

```
采集 → 理解 → 消化 → 溯源 → 判断 → 去重 → 安装 → 记录
```

每一步都可独立运行、可审计、可回滚。不是黑箱，是酿造工艺。

| 步骤 | 做什么 | 要点 |
|------|--------|------|
| ① 采集 | 粘贴素材（视频URL / 文件 / 报告 / 网页） | 多模态输入 |
| ② 理解 | 视频→字幕(ASR)+关键帧视觉融合；文件→直读；网页→抓取 | 真正「看」视频 |
| ③ 消化 | LLM 结构化理解 → 产出「执行计划」(分步、可授权) | 草稿即停（刹车） |
| ④ 溯源 | 识别引用源头(GitHub/Anthropic/论文)→取一手资料、就地纠正草稿 | 可选但推荐 |
| ⑤ 判断 | 给「值得装/不值得装/挑着装/整源跳过」主动建议 → 人定 | 只判「是否值得装」(D19) |
| ⑥ 去重 | 扫本地已装 + 比台账 → 重复归并/升级，非新增 | 只判「是否重复」(≠值得装) |
| ⑦ 安装 | 按步落地为能力产物(Skill/MCP/代码/配置/CLI/Docker/API/数据集)：安装命令**先查本地缓存→未命中查 catalog 种子→仍未命中由 AI 读源头仓库推断→试跑验证→缺项弹窗补全→验证过入缓存** → 写入台账 | 授权门坐这(D22/D23) |
| ⑧ 记录 | 生成 README 式安装记录(可分享、可回滚/移除) + 看板可视化 | 清楚知道 |

## 4. 关键决策表

| # | 议题 | 决定 | 状态 |
|---|------|------|------|
| D1 | 部署形态 | 不用 Coze 工作流；引擎跑在可执行环境（当前云电脑），代码开源同步 GitHub | ✅已定 |
| D2 | 能力产物形态 | 综合型：先消化成计划，再决定产物(Skill/MCP/代码/配置) | ✅已定 |
| D3 | 首批素材源 | B站 → 抖音 → YouTube（国内优先，纵向先跑通） | ✅已定 |
| D4 | 视频理解路线 | 字幕(ASR) + 关键帧视觉融合，非纯转文字 | ✅已定（路线 + 依赖已核实并**实测**：Agnes-1.5-Flash 真看图——手搓左红右蓝图识别正确；⚠️单图~5min，见第10节问题5） |
| D5 | 质量把关 | 半自动：AI 生成计划草稿 + 用户一键审核 | ✅已定 |
| D6 | 溯源 | 介绍源头类素材回原厂取一手；整合类不强求 | ✅已定 |
| D7 | 防臃肿 | 台账去重 + 归并 + 可移除 + 每次留记录 | ✅已定 |
| D8 | 开源 | 放 GitHub 共建 | ✅已定 |
| D9 | （并入 D12） | — | — |
| D10 | 与 whale 关系 | 新建独立仓库；复用 whale 获取层"逻辑"（抖音反爬/ASR/ffmpeg 经验），不混仓库、不抄 Node 代码 | ✅已定 |
| D11 | 技术栈 | 纯 Python（MVP 只做 B站避开抖音反爬；抖音逻辑参考 whale 用 Python Playwright 重写） | ✅已定 |
| D12 | 项目名 | skillbrew | ✅已定 |
| D13 | 消化模型 | **分角色已定**：文本(消化/计划)=DeepSeek(deepseek-chat,1–2s,已充值)；视觉(关键帧看图)=Agnes(agnes-1.5-flash,已测真看图但~5min/张)。依据=DeepSeek 官方 API 视觉命门(发图被网关替换成 `[Unsupported Image]` 占位符、秒回,6/18 全量上线仅网页端≠官方 API)。.env 已拆 TEXT_*/VISION_* 两组独立配置(D15 可插拔,换供应商只改配置)。后续 DeepSeek 官方开放 API 视觉,改一行 VISION_* 即可切回 | ✅已定 |
| D14 | 安全脱敏 | 开源硬约束：密钥走 .env(git忽略)，代码只读环境变量，仓库只放 .env.example 占位；开发初期即埋入 | ✅已定 |
| D15 | LLM 客户端 | 模型无关(provider-agnostic)：统一抽象层，base_url+api_key+model 全走 .env 配置；换供应商只改配置不改代码 | ✅已定 |
| D16 | 开放给他人 | 须提供 API key 输入位(.env/配置/交互)，让用户填自己的 key；不绑死 Agnes/DeepSeek 任何供应商 | ✅已定 |
| D17 | 分发文件格式 | **三层结构（2026-06-20）**：① L1 机器可读结构化块(YAML，放文件顶部，任何 Agent 解析即能获取/安装/登记/去重)；② L2 人读·死板·规矩·可分享正文。L1+L2 打包成一个分享文件(`SKILL_PACK.md`)，类比 README(人) vs package.json(机器) 两者都要。L3 口语化录像文案**单独**给 UP 主念稿录视频用(`VIDEO_SCRIPT.md`)，不进分享包 | ✅已定 |
| D18 | 去重基准 | **基准 = 扫描本机已装 skill，每台机器/每个 Agent 不同（2026-06-20）**：全新机基准=0 直接加；成熟 Agent 已装很多→门槛高、逐项比对、重叠的去芜存菁/整并、不重叠的登记。两条推论：① 分享文件的去重指令**不可硬编码比对目标**（须写"先扫本地 .claude/skills/ 建基准再逐项比"）；② skillbrew 去重模块须**扫本地优先→再比台账** | ✅已定 |
| D19 | 判断先行·协助人类判断 | 用户盲目丢素材→skillbrew 先消化+判断，给「值得装/不值得装/挑着装/整源跳过」**主动建议**再让人定，绝不盲目整装；dedup 只判「是否重复」≠「是否值得装」须分开；对配置商店/大集合源主动建议挑着买或整源跳过+候选清单+跳过理由 | 🎯已定 |
| D20 | 安装粒度·挑着买 | 大集合/配置商店源**禁整目录盲拷**（r20 davila7 判「整目录装 832」即荒谬）；install 须支持「单组件选装/购物车组合/npx 一键装」，install_method 由源形态决定（非写死 copy_whole_dir）；能力/产物抽象见 D2+记忆 project-artifact-not-just-skill。**【被 D23 部分取代】人工 catalog 降级为「预验证缓存种子」，不再人工扩充；新工具靠 D23 推断+验证自动入缓存** | 🎯方向已定（D23 部分取代） |
| D21 | 模型前置·文本必备·视觉可选优雅降级 | 使用本工具的**前置条件=至少 1 个大模型**（无 LLM 则消化步骤跑不动、工具完全不可用）；**任一品牌文本模型皆可**（所有 LLM 都有文字能力，不写死 DeepSeek）；**视觉模型为可选**——无视觉能力时**不卡死**，降级为「视频转语音→语音转文字」路径（识别质量较低、不能抽关键帧看画面），并主动建议换多模态模型但**不阻断**；配置=text_model(必填)+vision_model(可选)，契合 D15 热插拔/D16 开放他人；区别于 D13（D13=本机具体选型 DeepSeek+Agnes，D21=通用前置与降级规约） | 🎯已定 |
| D22 | 反盲盒·透明可追 | 直击「AI 全黑盒决定、人说好了但看不到、没安全感」的恐惧：① 消化素材后**必须**与人类交互确认才安装（强化 D19/D20 交互门，绝不黑盒自动装）；② 安装时说明每个功能**是什么**；③ 可视化报告须含「装了什么 / 新增哪些能力 / **以后怎么调用**」——尤其「调用方式」（@skill 名 / 触发提示词），让装了的能力能被找回、能被调用；呼应延后需求 R1（§9.1） | 🎯已定 |
| D23 | 通用安装器·推断-验证-缓存-补全 | 安装命令**不再以人工硬编码目录为唯一来源**（否则任何新 MCP 都得人手补目录，永无止境）。改为：① **推断**——AI 读源头仓库实际文件(README/package.json/pyproject.toml/Dockerfile/.env.example)推断装法(npx/uvx/docker/pip/git clone)+所需参数(key/路径/token/环境变量)；② **验证**——装前安全试跑(--help/--version/docker pull/import 检查)，能跑通才装，跑不通分析报错最多重试 1–2 次换装法，**不把没验证过的命令写进配置**；③ **补全**——缺 key/路径/token/环境变量时弹窗友善提示用户补，**绝不静默崩溃**(车子要跑就得有轮胎)，失败一律走友善报告不抛裸异常；④ **缓存**——验证过的装法写回本地缓存(gitignore，与 D14 一致)，下次同类工具命中缓存秒装。沿用 verify「纯一手探测、不调 LLM」原则，验证步骤本身确定性。**人工目录现有条目保留为「预验证缓存种子」**。先打通 MCP(真正瓶颈)，再按同一骨架逐步加 CLI/Docker/API/数据集 | 🎯方向已定（待编码） |

## 5. 技术路线

### 5.1 输入获取
- **B站**：yt-dlp 取视频 + CC字幕；无字幕则下载音频 → ASR
- **抖音**：解析分享链接取直链下载；字幕少 → ASR
- **文件/报告**：直接读取（pdf/docx/md/html）
- **网页**：抓取正文

### 5.2 视频理解（关键 —— 回应"真正看视频"）
- **默认路线 = 字幕(ASR) + 关键帧视觉融合**
  - ASR(中文)：faster-whisper / funasr / whisper.cpp
  - 关键帧：~~ffmpeg 场景切换检测抽帧（`select='gt(scene,0.4)'`）~~【实测失效：本 360p 短视频阈值 0.20 / 0.02 均抽出 0 帧，全程场景分极低】→ 改用 **PIL 帧间差异签名(32×24 灰度) + farthest-point 采样**：等距抽缩略图算灰度签名，贪心选互相差异最大的 N 帧(默认 5，限帧策略 A)、强制 min_spacing=5s 防扎堆。已落地 `understand.select_keyframes`
  - 视觉：关键帧 + 字幕一起送多模态 LLM（首选 Agnes-1.5-Flash；备选 Qwen-VL / Gemini / Claude）→ 看懂画面里的代码/图/UI/幻灯片
- **可选升级**：原生视频输入模型（Gemini 2.0 / Qwen-VL 直接吃视频）
- **纯字幕仅作降级方案**

### 5.3 溯源
- 从消化结果抽实体：GitHub repo / Anthropic 报告 / arXiv / 文档 URL
- 标注"一手溯源型" vs "二手整合型"
- **GitHub 一手取法（2026-06-20 实测）**：`gh` 未登录、无 GITHUB_TOKEN；WebFetch 对 `github.com` 被 claude.ai 安全策略挡。可通路径=**curl 直连 `api.github.com`**（同 B站 412 的绕过逻辑，直网可出）：① `/search/repositories?q=...` 搜仓库（**独立限速桶 10/min**，与核心 API 60/hr 不挤）；② `/repos/{owner}/{repo}` 核实存在+星数+默认分支；③ `/repos/{o}/{r}/git/trees/main?recursive=1` 拉全文件树（列全部 SKILL.md 路径）；④ **`raw.githubusercontent.com` 取静态文件不耗 API 限额**（拿 README / SKILL.md 正文用它最稳）。公开仓无需鉴权。用此法核实过 `mattpocock/skills`。
- **刻舟求剑方法论**：溯源取回的任何**计数型元数据**（星数、skill 数、本地已装数）都是**动态线索、非永久事实**——星数日日涨、本地 skill 随装随删；把一个数字写进文档当定值 = 刻舟求剑。规矩：① 星数等动态值须标**取数时点**（`stars_observed_at`），仅作"顺藤摸瓜定位"用，勿当定值引用；② 本地已装数须**实时扫描**（D18），不可引用旧文档里的旧数；③ 发现旧文档数字与实测不符，就地纠正（"把错的资料变正确的"）。曾出现过去重基准的误记，实扫后纠正。④ **验证须一手直查**（绝对路径 / 直接读盘），不可凭一次失败的探测就判定"不存在"——一次相对路径探针因 Bash 工作目录(CWD)不在工作区根而误报"`registry.db` 不存在"，险些据此推翻已真实落地的台账、去"纠正"本就正确的文档。教训：核验一律绝对路径直查；失败探针先复核根因（CWD / 路径拼写 / 权限）再采信，勿因一次探测失败就否定已落地事实。该假警报经绝对路径直查解除。

### 5.4 能力产物落地

先生成「执行计划」（中间表示），再按内容选产物形态：

| 形态 | 落地位置 | 安装方式 | 示例 |
|------|---------|---------|------|
| **Skill** | `.claude/skills/<name>/` | 整目录拷贝（含 SKILL.md） | mattpocock/skills 的 34 个技能 |
| **MCP** | `~/.claude.json` 或 `.mcp.json` | `claude mcp add` 或 JSON 合并 | playwright / filesystem / context7 |
| **代码** | 项目内模块 | 直接写入 | 自定义脚本、工具函数 |
| **配置** | `CLAUDE.md` 或片段库 | 追加或覆盖 | 项目级指令、代码规范 |
| **Prompt** | 片段库或 Skill | 模板化存储 | 提示词模板、工作流模板 |
| **CLI 工具** | 系统级 / 全局 bin | npm/pip 全局装，验证 `--version` | 命令行工具、脚本入口 |
| **Docker 镜像** | 本地 Docker | docker pull / run，装前 docker pull 验证 | 容器化服务、隔离环境 |
| **API 服务** | 配置块 + .env | 生成配置模板，弹窗要 key | 第三方 API 接入、远程服务 |
| **数据集 / 模板** | 本地数据目录 | 下载 / 落库 | 数据集、Prompt 模板库 |

> **关键设计**：形态由计划内容决定（D2），不预设。install_method 由源形态决定（D20），不硬编码 copy_whole_dir；具体装法优先查缓存→catalog 种子→AI 推断+验证（D23），不依赖人工硬编码目录。

## 6. 部署形态分析（Coze vs 本地）

**核心判断**：工具的目的是"让 AI 真实获得能力"，能力必须落到 **AI 运行时所在处**。
- Claude Code / Codex 运行在用户**本地** → 能力须落本地（`.claude/skills/` 等）
- Coze 云 Agent 跑在**云端沙箱** → 无法写用户本地任意路径（早前 PATH_OUTSIDE_WORKSPACE 已验证边界）

⇒ **落地点必须在本地**，这是硬约束。

**推荐**：本地 CLI 为核心引擎（消化/计划/安装/台账/看板全在本地，开源可克隆运行）；Coze 为可选门面（可作为"前台"：用户在对话里贴 URL，由 Coze 编排消化、呈计划待授权，再由本地引擎落地；Coze 也适合当看板查看器）。

**耦合度两档**：
- v1 松耦合：对话里给计划+产物，本地手动跑安装（最简、最先开源）
- v2 紧耦合：本地引擎开 HTTP，Coze Agent 直接调（体验顺，但要本地常驻）

## 7. 架构

### 7.1 目录结构

```
skillbrew/
  ingest/      采集 + 获取（各源 adapter；抖音反爬逻辑参考 whale，用 Python Playwright 重写）
  understand/  ASR + 关键帧 + 视觉融合
  verify/      溯源（Skill: resolve_repo / MCP: mcp_catalog.lookup）
  plan/        消化 → 执行计划（含形态标注 form=Skill/MCP/Code/Config）
  install/     落地各形态产物（Skill: 整目录拷 / MCP: claude mcp add）
  registry/    能力台账（去重/归并/查询/移除）
  record/      安装记录（RECORD.md 台账 + DASHBOARD.md 看板）
  dashboard/   累加可视化
  cli/         命令入口
```

### 7.2 8 步管线数据流

```
┌─────────────────────────────────────────────────────────────────┐
│                        输入源（多源采集）                         │
│  B站视频 / 抖音 / YouTube / 文件 / 网页 / 报告                   │
└──────────────────────────┬──────────────────────────────────────┘
                           │ Step 1: 采集（ingest）
                           ▼
┌─────────────────────────────────────────────────────────────────┐
│  Step 2: 理解（understand）                                      │
│  ├─ 文本组：字幕提取 + 文本理解（DeepSeek）                       │
│  └─ 视觉组：关键帧截图 + 视觉理解（Agnes）                       │
└──────────────────────────┬──────────────────────────────────────┘
                           │
                           ▼
┌─────────────────────────────────────────────────────────────────┐
│  Step 3: 消化（plan）                                            │
│  输出：plan.json（执行计划 + 能力列表 + 形态标注）                 │
│  关键：形态由内容决定（D2），不预设                                │
└──────────────────────────┬──────────────────────────────────────┘
                           │
                           ▼
┌─────────────────────────────────────────────────────────────────┐
│  Step 4: 溯源（verify）                                          │
│  ├─ Skill：resolve_repo() 解析 GitHub 仓库                      │
│  └─ MCP：mcp_catalog.lookup() 查 brew-formula 表                │
│  输出：install_list.json（含 form/repo/stars/usability）         │
└──────────────────────────┬──────────────────────────────────────┘
                           │
                           ▼
┌─────────────────────────────────────────────────────────────────┐
│  Step 5: 判断（recommend）                                       │
│  LLM 打分：值不值得装？为什么？                                   │
│  输出：recommendations.json（含 score/reasoning/usability）      │
└──────────────────────────┬──────────────────────────────────────┘
                           │
                           ▼
┌─────────────────────────────────────────────────────────────────┐
│  Step 6: 去重（dedup）                                           │
│  3 层从严判定（D18）：名字 → 描述 → 语义                          │
│  输出：dedup_report.json（new/duplicate/ambiguous）              │
└──────────────────────────┬──────────────────────────────────────┘
                           │
                           ▼
┌─────────────────────────────────────────────────────────────────┐
│  Step 7: 安装（install）                                         │
│  ├─ 授权门（D19）：默认 dry-run，--approve 才真装                │
│  ├─ Skill：copy_whole_dir → ~/.claude/skills/                   │
│  └─ MCP：claude mcp add 或 JSON 合并 → ~/.claude.json           │
└──────────────────────────┬──────────────────────────────────────┘
                           │
                           ▼
┌─────────────────────────────────────────────────────────────────┐
│  Step 8: 记录（record）                                          │
│  ├─ RECORD.md：台账（每个能力的来源/时间/调用方式）                │
│  ├─ DASHBOARD.md：看板（可视化统计）                              │
│  └─ 就绪状态列：装完前必做（凭证/配置/首次运行）                      │
└─────────────────────────────────────────────────────────────────┘
```

### 7.3 关键模块映射

| 步骤 | 代码文件 | 核心函数 | 产出 artifact |
|------|---------|---------|--------------|
| 1. 采集 | `ingest.py` | `ingest()` | `sources/` 目录 |
| 2. 理解 | `understand.py` | `understand()` | `transcript.json` / `keyframes/` |
| 3. 消化 | `plan.py` | `plan()` | `plan.json` |
| 4. 溯源 | `verify.py` | `verify()` | `install_list.json` |
| 5. 判断 | `recommend.py` | `recommend()` | `recommendations.json` |
| 6. 去重 | `dedup.py` | `dedup()` | `dedup_report.json` |
| 7. 安装 | `install.py` | `install()` | 实际能力（Skill/MCP/...） |
| 8. 记录 | `record.py` | `record()` | `RECORD.md` / `DASHBOARD.md` |

### 7.4 分支决策点

**Step 4（溯源）开始分流**：

```
plan.capabilities[].form == ?
├─ "Skill" → resolve_repo() → 整目录拷
├─ "MCP" → mcp_catalog.lookup() → claude mcp add
├─ "Code" → 直接写入项目模块
└─ "Config" → 追加/覆盖配置文件
```

> **设计原则**：形态由计划内容决定（D2），install_method 由源形态决定（D20），不硬编码。

## 8. 框架 / 脚手架 / 依赖

### 8.1 技术栈总览

| 层级 | 技术选型 | 说明 |
|------|---------|------|
| **语言** | Python 3.10+ | 生态最适合：yt-dlp / whisper / ffmpeg / PDF；单语言降门槛 |
| **CLI** | argparse（内置） | 轻量，无需额外依赖 |
| **LLM 调用** | openai SDK | 兼容 OpenAI API 的供应商（DeepSeek / Agnes / Claude） |
| **视频获取** | yt-dlp | B站 / 抖音 / YouTube 多源支持 |
| **ASR** | faster-whisper | 本地字幕提取，离线可用 |
| **视觉理解** | Agnes API | 关键帧看图（OCR / 图表 / UI） |
| **文本理解** | DeepSeek API | 消化 / 执行计划 / 判断 |
| **台账存储** | JSON + Markdown | 轻、可读、可 git 追踪 |
| **看板** | Markdown + Mermaid | 纯文本，易分享 |
| **配置** | pyproject.toml + .env | 标准 Python 包配置 + 环境变量 |

### 8.2 核心依赖（已验证）

| 包 | 版本 | 用途 | 安装方式 |
|---|------|------|---------|
| `openai` | ≥1.0 | LLM 调用（DeepSeek / Agnes） | 必装 |
| `yt-dlp` | 2026.06.09+ | 视频下载 | 可选 `pip install -e ".[media]"` |
| `faster-whisper` | 1.2.1+ | ASR 字幕提取 | 可选 `pip install -e ".[media]"` |
| `ffmpeg` | 4.4.2+ | 视频/音频处理 | 系统级 `apt install ffmpeg` |

### 8.3 可选依赖

```bash
# 基础安装（仅 LLM 调用）
pip install -e .

# 完整安装（含视频获取 + ASR）
pip install -e ".[media]"

# 开发安装（含测试）
pip install -e ".[dev]"
```

### 8.4 云电脑环境坑（已踩并解决）

**问题**：云电脑（非工作流 Python 沙箱）**可装软件、有 pip 权限**；但到 pypi.org 的链路慢，大依赖（如 onnxruntime 35MB）下载反复超时会 hang 死进程（曾导致安装"卡死"被强制终止）。

**解法**：pip 全局走阿里云镜像
```ini
# /etc/pip.conf
[global]
index-url = https://mirrors.aliyun.com/pypi/simple/
timeout = 60
retries = 5
```

**已装齐环境**：
- Python 3.13.12
- yt-dlp 2026.06.09
- openai 2.43.0
- faster-whisper 1.2.1
- ffmpeg 4.4.2

### 8.5 Agnes 接入速查（外部依赖，视觉首选用）

- **注册**：https://platform.agnes-ai.com，邮箱注册，无需绑卡 / 手机号 / 实名
- **拿 key**：控制台 → API Keys → 创建新密钥 → 复制 `sk-` 开头 key（⚠️只显示一次，丢了只能重建）
- **接入参数**：base_url=`https://apihub.agnes-ai.com/v1`；走 OpenAI 兼容 SDK(openai 库)；认证 Bearer Token
- **模型分工**：agnes-2.0-flash = 字幕消化+执行计划(文本)；agnes-1.5-flash = 关键帧视觉(多模态，直接发 image_url，**无需任何开关**)
- **免费但有坑**：官方"核心模型无限期免费"、实测不扣 token；高峰期可能慢/排队；对 MVP(字幕+关键帧)影响小
- **可插拔**：换 DeepSeek / Qwen-VL / Gemini 只改 .env（D15），代码不动
- **诚实标注**：官方文档站(agnes-ai.com/doc)需 JS 渲染、coze web-fetch 抓不到正文，以上信息来自官方文档正文片段+多篇第三方教程交叉核实，注册控制台实际界面以你登录后看到的为准
- **实测结果（2026-06-19，真 key 冒烟测试 scripts/smoke_test.py）**：
  - ✅ key/base_url/auth 全通；`/v1/models` 返回 5 个确切 id：agnes-1.5-flash / agnes-2.0-flash / agnes-image-2.0-flash / agnes-image-2.1-flash / agnes-video-v2.0
  - ✅ 文本 agnes-2.0-flash：7.5s，自报"由 Sapiens AI 开发的 Agnes-2.0-Flash"
  - ✅ 视觉 agnes-1.5-flash：**真看图**——手搓「左红右蓝」PNG，正确答出"红色和蓝色分别位于图像左右两侧"
  - ⚠️ 视觉单图耗时 298.9s(~5min)，"慢也是真的"被实测坐实；批量关键帧须限帧+后台异步（见第10节问题5）。**两次实测稳定:244.6s / 298.9s,文本 1.3–7.5s,慢是稳定现象非偶发**

## 9. 里程碑

### 9.1 已完成

#### ✅ MVP（2026-06-21 完成）

**目标**：证明闭环跑通

- ✅ B站单源（BV1UpR9BBEf5：mattpocock/skills 34 个技能）
- ✅ 字幕 + 关键帧消化 → 执行计划
- ✅ 人工审核门（dry-run 默认）
- ✅ 装 Skill → 台账 + 单次报告

#### ✅ v1（2026-06-21 完成）

**目标**：多源 + 多形态 + 判断 + 去重 + 可视化

- ✅ 抖音源支持（7650401218518387995：6 个 MCP 推荐视频）
- ✅ **多形态产物**：Skill + MCP（核心突破，不再退化成 Skill 加载器）
- ✅ GitHub 一手溯源（resolve_repo + mcp_catalog）
- ✅ **自动评估**（`recommend` 输出值得装 / 挑着装 / 整源跳过）
- ✅ 3 层去重（名字 / 描述 / 语义）
- ✅ 累加看板（DASHBOARD.md）+ 台账（RECORD.md）
- ✅ 安装记录 + 回滚支持
- ✅ 安装后 readiness 标注（凭证 / 配置 / 首次运行）
- ✅ **双运行时中立**（Claude Code / Codex CLI 自动识别 + 路径可覆盖）

**已真装验证的 MCP 服务器**（5 个）：
1. `@playwright/mcp` — 浏览器自动化
2. `@modelcontextprotocol/server-filesystem` — 文件系统访问
3. `@modelcontextprotocol/server-sequential-thinking` — 结构化思考
4. `@upstash/context7-mcp` — 官方文档查询
5. `@modelcontextprotocol/server-github` — GitHub 操作（已配置 PAT 并通过冒烟测试）

**未默认安装（依赖或凭证未就绪）**：
- Memos MCP（无官方 MCP，候选 `@modelcontextprotocol/server-memory`，暂不安装）
- MoneyPrinterTurbo（还缺 OPENAI_API_KEY，暂不配置）

### 9.2 规划中

#### 📋 v1.5（规划中）通用安装器（推断-验证-缓存-补全）

**目标**：把安装命令的来源，从「人工硬编码目录」转向「AI 读源头仓库推断 + 试跑验证 + 缺项弹窗补全 + 验证过入缓存」。对应决策 D23。

- ⏳ **推断**：AI 读源头仓库文件(README/package.json/pyproject.toml/Dockerfile/.env.example)→ 推断装法(npx/uvx/docker/pip/git clone)+所需参数(key/路径/token/环境变量)
- ⏳ **验证**：装前安全试跑(--help/--version/docker pull/import 检查)，能跑通才装，失败最多重试 1–2 次换装法
- ⏳ **补全**：缺 key/路径/token/环境变量 → 弹窗友善提示用户补，**绝不静默崩溃**，失败走友善报告
- ⏳ **缓存**：验证过的装法写回本地缓存(gitignore)，下次同类工具命中缓存秒装；人工 catalog 降为「预验证缓存种子」
- ⏳ **先打通 MCP**（当前硬编码目录的真正瓶颈），再按同一骨架逐步加 CLI / Docker / API / 数据集

#### 🔄 v2（规划中）

**目标**：多源扩展 + 分享 + 门面

- ⏳ YouTube 源支持
- ⏳ Coze 门面（扣子商店集成）
- ⏳ GitHub 开源包装（README / 文档 / 示例）
- ⏳ 分享机制（RECORD.md 导出 / 能力包分享）

#### 📋 v3+（远期）

- ⏳ 能力调度器（自动加载已装能力，见 §9.3）
- ⏳ Web UI（可视化操作界面）
- ⏳ 社区能力库（用户贡献 + 审核 + 分发）

### 9.3 延后需求（记录待后续版本）

- **R1 能力调度/自动加载器（v3+）**：用户痛点（2026-06-20 提出）——skill 装了一堆但 Agent 不会自动调用，须 `@skill` 或读其 prompt 才触发，否则装 1000 个也不调。这是与「安装」正交的另一层：**调度器**（任务执行前扫自己已装能力→判该加载哪些→加载→再执行），非 skillbrew 安装器的职责。MVP 桥接：报告/台账带「调用方式」（@名/触发提示词，见 D22）让人工/Agent 能手动调；完整自动调度留 v3+ 独立组件。

## 10. 开放问题（历史决策记录）

### ✅ 已解决

1. **D1 部署** ✅（2026-06-19）：不用 Coze 工作流；引擎跑可执行环境，代码同步 GitHub
2. **MVP 范围** ✅（2026-06-19）：B站单源 + 跑通闭环
3. **项目名** ✅（2026-06-19）：skillbrew（技能酿造局）
4. **D4 依赖** ✅（2026-06-19）：yt-dlp + faster-whisper 装机；视觉走 Agnes-1.5-Flash，经 D15 可插拔
5. **Agnes 视觉慢** ✅（2026-06-20）：限帧策略已定（A 限帧 + B 并发 + C 选择性），先 A+C 跑通闭环

### 🔄 待探索

6. **Agnes 官方限速**：批量关键帧并发需查清每分/每秒请求限额，在限额内用
7. **YouTube 源支持**：v2 规划，需调研 yt-dlp 对 YouTube 的反爬策略
8. **Coze 门面集成**：v2 规划，如何把 skillbrew 能力包装成扣子商店可分发的形态

6. **模型供应商——✅已拍板分角色（2026-06-19）**：文本=DeepSeek、视觉=Agnes（依据=D13 + 下方命门实测）。.env 已拆 TEXT_*/VISION_* 两组独立配置，skillbrew 骨架开建。
   - **DeepSeek 视觉（实测命门，2026-06-19，真 key 冒烟测）**：官方 API **不支持视觉**。OpenAI 端点(`/v1/chat/completions` + `image_url`)直接报 400 "unknown variant image_url, expected text"；Anthropic 兼容端点(`/anthropic/v1/messages` + Anthropic 图片块)虽返回 200，但模型称看不到图、并直指网关把图替换成了 **`[Unsupported Image]` 占位符**；base64 与公网 URL 两种传图方式结果一致、且秒回（真看图会慢）。结论：**6/18 全量上线的是网页端「识图模式」，官方 API 尚未开放视觉**。第三方中转(napiai.com)能跑通是中转层另走通道，非官方 API。
   - **DeepSeek 文本（实测可用）**：deepseek-chat 1–2s、自报"由深度求索创造的 DeepSeek"，已充值。字幕消化+执行计划用 DeepSeek 文本完全胜任。
   - **Coze 尊享版**（已查清，维持）：Bot 中心、无原生模型 API、不适配 D15，且与 D16 冲突 → MVP 不考虑。
   - **分角色已定（2026-06-19）**：**分角色**——文本(消化/计划)=DeepSeek（快、便宜、已充值）；视觉(关键帧看图)=Agnes（唯一已测真看图的官方 API）。这正是 D15 可插拔设计的价值：不同环节各取所长，不赌单一供应商。.env 已拆成 TEXT_*/VISION_* 两组独立配置（DeepSeek 与 Agnes 的 base_url+key 不同，已落地）。Agnes 视觉~5min/张的慢，靠已定的限帧 A+C 缓解。后续 DeepSeek 一旦开放官方 API 视觉，改一行 VISION_* 即可切回。

## 11. 重大决策摘要

> 完整的开发过程与逐轮修订记录已归档至本地开发笔记，公开仓库仅保留决策摘要。

| 日期 | 涉及决策 | 一句话摘要 |
|---|---|---|
| 2026-06-19 | D1 / D10 / D11 / D13 | 确定本地优先、独立仓库、纯 Python、分角色模型方案 |
| 2026-06-19 | D4 / D14 / D15 / D16 | 视觉走 Agnes、安全脱敏、模型可插拔、开放给他人填 key |
| 2026-06-20 | D17 / D18 | 三层分发格式、去重基准 = 扫本地已装 |
| 2026-06-20 | D19 / D20 / D21 / D22 | 判断先行、挑着买、模型前置与降级、反盲盒透明可追 |
| 2026-06-20 | — | 8 步管线各模块（run / verify / dedup / install / record）陆续落地 |
| 2026-06-21 | — | 文档与架构可视化增强，MVP / v1 完成 |
| 2026-06-22 | — | 运行环境中立化改造完成（Claude Code / Codex CLI），仓库转为 PUBLIC |
| 2026-06-23 | — | README / 章程 / 落地页同步真实现状，启用 GitHub Pages |
| 2026-06-24 | D23 | 通用安装器方向定调：安装命令从人工硬编码目录转向「AI 推断 + 验证 + 缺项补全 + 入缓存」，D20 的人工 catalog 降为「预验证缓存种子」（待编码，§9.2 v1.5） |

